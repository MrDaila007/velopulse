# 05. Аппаратная часть

Требования-источники: ТЗ §2, §35.

---

## 1. Состав (BOM)

| № | Компонент | Рекомендация | Кол-во | Примечание |
| --: | --- | --- | --: | --- |
| 1 | Микроконтроллерный модуль | NologoTech Super-nRF52840 | 1 | Форм-фактор и bootloader XIAO, nRF52840, BLE 5, зарядка Li-Po |
| 2 | Дисплей | OLED SSD1306 128×64 или 128×32, I²C, белый | 1 | Адрес `0x3C` (перемычка на `0x3D` допустима) |
| 3 | Датчик Холла | TI **DRV5032** — вариант с частотой опроса **2.5 кГц** (напр. DRV5032AJ/DU) | 1 | ~5–6 мкА, цифровой выход. **Варианты с опросом 20 Гц (FA/FB) непригодны** — см. §2.3 |
| 3а | Альтернатива (прототип) | A3144 / AH3362 / TLE4913 | 1 | Непрерывный режим, но ток покоя 1.5–8 мА |
| 3б | Не рекомендуется | 49E (аналоговый) | — | Только временный прототип (ТЗ §2.3) |
| 4 | Магнит | Неодимовый N35+, диск 6×3 мм или велосипедный магнит на спицу | 1 | В корпусе-держателе на спицу |
| 5 | Аккумулятор | Li-Po 1S, 500–1000 мА·ч, с защитой (PCM) | 1 | Например 503035 (500 мА·ч) или 603048 (1000 мА·ч) |
| 6 | Резистор подтяжки | 4.7–10 кΩ | 1 | Только если не используется `INPUT_PULLUP` |
| 7 | Конденсатор | 100 нФ по питанию датчика | 1 | Рядом с датчиком |
| 8 | Кабель к датчику | 3 жилы, экранированный или витой, 40–60 см | 1 | Разъём JST-SH 3-pin для съёмности |
| 9 | Корпус компьютера | Печать PETG/ASA, IP54 | 1 | Крепление на руль хомутом/резинкой |
| 10 | Корпус датчика | Печать PETG, хомут на вилку | 1 | Регулировка зазора |
| 11 | Крепёж | Стяжки, резиновые прокладки, винты M2 | — | — |
| 12 | Фоторезистор | Аналоговый LDR, калибруется на собранном экземпляре | 1 | Автоматическая яркость OLED |
| 13 | Резистор делителя LDR | 22 кΩ, при калибровке допустимы 10/47 кΩ | 1 | Подбор под фактический LDR |
| 14 | Конденсатор фильтра LDR | 100 нФ | 1 | Между ADC_NODE и GND |

Обоснование выбора датчика: DRV5032 (1.8–5.5 В, микроамперное потребление, цифровой выход)
снимает главную проблему классического A3144 — постоянный ток 5–8 мА, который на батарее
500 мА·ч даёт менее 4 суток простоя. Полный разбор в ADR-002.

### 1.1. Требование к частоте опроса датчика (критично)

Низкопотребляющие Hall switch'и работают в импульсном режиме: внутренний генератор
периодически включает чувствительный элемент. Магнит находится в зоне обнаружения очень
недолго, поэтому частота опроса задаёт нижнюю границу пригодности датчика.

```text
Оценка окна обнаружения:
  зона срабатывания по дуге          ≈ 20 мм (магнит 6 мм + гистерезис по обе стороны)
  линейная скорость магнита на радиусе установки при 30 км/ч  ≈ 8.3 м/с
  длительность окна = 0.020 / 8.3    ≈ 2.4 мс
  при 50 км/ч                        ≈ 1.4 мс
```

Отсюда требования при выборе конкретного варианта датчика:

* частота внутреннего опроса **≥ 1 кГц** (окно 1.4 мс должно попасть минимум на один опрос);
* варианты DRV5032 с опросом 20 Гц (период 50 мс) **пропустят почти каждый проход магнита** —
  использовать нельзя, несмотря на привлекательные 0.54 мкА;
* варианты с опросом 2.5 кГц (период 0.4 мс) дают 3–6 опросов за окно — надёжно;
* непрерывные (не импульсные) датчики вроде A3144 по частоте подходят всегда, вопрос только
  в потреблении.

При закупке проверяется по datasheet: `Sampling rate` / `Output update rate` конкретного
суффикса варианта. Значение фиксируется в `hardware/bom.md`. Проверка на стенде —
пункт 6 таблицы §7 (отсутствие потерь импульсов на эквиваленте 60 км/ч).

Магнит увеличенного размера (или два магнита, расположенных рядом как один «широкий») окно
удлиняет, но менять число магнитов на колесе нельзя — один импульс должен соответствовать
одному обороту (ТЗ §2.4).

## 2. Схема соединений

```text
         Li-Po 3.7 V                          ┌──────────────────────────┐
         ┌──────┐                             │  OLED SSD1306 128×64/32  │
         │  +   ├──────── BAT+ ──┐            │  VCC  GND  SCL  SDA      │
         │  −   ├──────── BAT− ──┤            └───┬────┬────┬────┬───────┘
         └──────┘                │                │    │    │    │
                                 │            3V3 ┘    │    │    │
     ┌───────────────────────────▼────────────┐        │    │    │
     │      Super-nRF52840                    │        │    │    │
     │                                        │        │    │    │
     │  3V3 ─────────────────────────────┬────┼────────┘    │    │
     │  GND ─────────────────────────────┼────┼─────────────┘    │
     │  D5 / SCL ────────────────────────┼────┼──────────────────┘    │
     │  D4 / SDA ────────────────────────┼────┼───────────────────────┘
     │                                   │    │
     │  D0  ◀─── OUT ── Hall sensor      │    │
     │  3V3 ───── VCC ── (DRV5032)       │    │
     │  GND ───── GND ──                 │    │
     │                                   │    │
     │  D3 ── LDR ──┬── D2/A2            │    │
     │               ├── 22 kΩ ── GND     │    │
     │               └── 100 nF ── GND    │    │
     │                                   │    │
     │  USB-C: зарядка + прошивка + Serial    │
     └────────────────────────────────────────┘

   Подтяжка D0: внутренняя INPUT_PULLUP (по умолчанию)
              либо внешний 4.7–10 кΩ на 3V3 (для длинного кабеля предпочтительно)
   Дополнительно: 100 нФ между VCC и GND у датчика
```

### 2.1. Таблица подключений

| Сигнал | Пин платы | Назначение | Примечание |
| --- | --- | --- | --- |
| SDA | D4 | I²C данные (дисплей) | ТЗ §2.2 |
| SCL | D5 | I²C такт (дисплей) | ТЗ §2.2 |
| HALL_OUT | D0 | Вход импульсов, GPIO IRQ | `INPUT_PULLUP`, активный фронт FALLING |
| 3V3 | 3V3 | Питание дисплея и датчика | Суммарно < 30 мА |
| GND | GND | Общий | — |
| LIGHT_POWER | D3 / P0.29 | Коммутируемое питание LDR | HIGH только во время выборки |
| LIGHT_ADC | D2/A2 / P0.28 | Узел LDR / R_FIXED / 100 нФ | 12-bit SAADC |
| VBAT_ADC | задняя площадка P0.31 | Середина внешнего делителя 1 MΩ / 1 MΩ | См. §3 |
| CHG_STATUS | недоступен | LED зарядки подключён только к контроллеру | В ПО `charge_status_unknown` |

### 2.2. Проверенная распиновка Super-nRF52840

Фактическая плата — NologoTech Super-nRF52840, использующая bootloader XIAO. На схеме
производителя предусмотрены позиции `NPR1/NPR3` для делителя через P0.03/P0.04, однако
аппаратная проверка дала `raw=505…598` с разбросом до 469 отсчётов: цепь конкретной платы
не установлена или непригодна для измерения совместно с OLED SDA на D4.

Для рабочего варианта используется внешний постоянный делитель:

```text
BAT+ → 1 MΩ → P0.31 (задняя площадка, logical pin 32) → 1 MΩ → GND
```

Ток делителя при 4.2 В ≈ 2.1 мкА. P0.31 присутствует в pin map XIAO как logical pin 32
и физически выведен на заднюю сторону Super-nRF52840. Статус зарядки аппаратно не выведен
на MCU: выход контроллера управляет только LED. Точная калибровка scale/offset по
мультиметру остаётся обязательной перед релизом.

## 3. Измерение напряжения аккумулятора

* Внешний делитель: 1 MΩ от BAT+ к P0.31 и 1 MΩ от P0.31 к GND; коэффициент 2.0.
* ADC: logical pin 32 / P0.31, 12 бит, `AR_INTERNAL_2_4`, диапазон 0…4095, acquisition time 40 мкс.
* Раз в секунду выполняются 16 выборок, min/max отбрасываются, остальные 14 усредняются.
* Пересчёт, калибровка и EMA выполняются целочисленно; применяется компенсация OLED +25 мВ.
* По умолчанию `scale_permille=1000`, `offset_mv=0`; точность ±50 мВ подтверждается позже
  сравнением с мультиметром при трёх уровнях заряда.

Паять делитель можно только при отключённых USB и аккумуляторе. Перед подачей питания
проверяются сопротивление между BAT+/GND и отсутствие короткого замыкания P0.31 на питание.

### 3.1. Датчик внешнего освещения

Фоторезистор питается от D3 только на время измерения:

```text
D3/P0.29 → LDR → ADC_NODE → R_FIXED → GND
                       ├── D2/A2/P0.28
                       └── 100 нФ → GND
```

Начальное значение `R_FIXED` — 22 кΩ. Для неизвестного LDR сопротивление проверяется
мультиметром при закрытом датчике, комнатном освещении и на улице. Если полезный
12-bit ADC-диапазон заметно уже целевых `raw=100…3900`, резистор заменяется на 10 или
47 кΩ. После выбора резистора фактические `raw_dark` и `raw_bright` фиксируются как
калибровочные константы firmware; `raw_bright` должен быть больше `raw_dark`.

Прошивка раз в секунду поднимает D3, неблокирующе ждёт стабилизации RC-цепи, снимает
16 выборок с отбрасыванием min/max и снова переводит D3 в LOW. При времени питания
не более 50 мс и токе делителя не более 0.3 мА средний дополнительный ток не превышает
15 мкА. Датчик устанавливается наружу рядом с OLED, под козырьком от засветки самим
экраном и за прозрачным герметичным окном.

Флаг `BIKECOMP_AMBIENT_LIGHT=1` используется production-сборками обеих высот.
Значение `0` оставляет ручное управление яркостью для старого железа без LDR.
Параметр `brightness_pct` в `DeviceConfig` остаётся верхним пределом автояркости;
BLE wire format не меняется.

## 4. Зарядка

* На Super-nRF52840 установлен отдельный контроллер зарядки Li-Po и индикаторный LED.
* Ток задаётся аппаратной обвязкой платы; прошивка его не переключает.
* Выход статуса зарядки не подключён к nRF52840, поэтому протокол сообщает
  `charge_status_unknown`. Наличие VBUS определяется MCU и используется только для
  разрешения роста фильтрованного процента.
* Зарядка и фактический ток проверяются отдельно USB power meter перед релизом.

## 5. Механика установки

### 5.1. Датчик на вилке

```text
       вилка                     спица
         │                         │
    ┌────┴────┐             ┌──────┴──────┐
    │ датчик  │◀── 2..8 мм ─│   магнит    │
    │ (метка) │             │ (метка ↕)   │
    └─────────┘             └─────────────┘
    метки маркированных сторон должны быть обращены друг к другу
```

Требования (ТЗ §2.4):

* Зазор 2–8 мм; целевое значение **3–5 мм** — компромисс между надёжностью срабатывания
  и риском контакта при биении колеса.
* Отсутствие механического контакта при максимальном биении: проверяется прокруткой колеса
  и покачиванием.
* Датчик ориентирован чувствительной стороной к магниту; для униполярного DRV5032FA важна
  полярность магнита — если срабатываний нет, магнит переворачивается.
* Один магнит на колесо → один импульс на оборот (ТЗ §2.4). Установка двух магнитов
  недопустима без изменения прошивки.
* Кабель фиксируется стяжками с петлёй запаса у выхода из корпуса (защита от вырывания),
  не касается тормозного диска и покрышки.

### 5.2. Корпус на руле

* Крепление на руль 22.2 мм: хомут или силиконовые стяжки; поворот дисплея не требуется.
* Дисплей должен читаться при типовом угле обзора сверху 30–60°.
* Класс защиты: минимум IP54 (дождь, брызги). Разъём USB-C закрыт заглушкой/мембраной.
* Внутри — виброизоляция АКБ (поролон 2 мм) и разгрузка проводов.
* Антенна nRF52840 на модуле не должна прилегать к металлу; в корпусе обеспечить
  зазор ≥ 5 мм от металлических элементов руля.

## 6. Энергетический бюджет

| Режим | Компоненты | Ток |
| --- | --- | --- |
| Движение | MCU active + OLED вкл + BLE медленная реклама | 8–12 мА |
| Короткая остановка | то же, обновление 1 Гц | 6–9 мА |
| Idle, OLED выкл | MCU sleep между тиками + реклама 1000 мс | 0.6–1.5 мА |
| Подключение BLE | + активное соединение 15–60 мс | +1–3 мА |
| Deep sleep (если включён) | System OFF + GPIO SENSE | 10–30 мкА |
| Зарядка | — | вход 100 мА |

| LDR, среднее при 1 выборке/с | Делитель включён не более 50 мс | ≤ 20 мкА |
Расчётная автономность при 500 мА·ч:

| Сценарий | Оценка |
| --- | --- |
| Непрерывная езда | 40–60 ч |
| 1 ч езды в день + остальное idle (OLED выкл) | ~2 недели |
| Хранение с deep sleep | месяцы |
| Хранение без deep sleep (idle 1 мА) | ~20 дней |

Вывод: без глубокого сна устройство остаётся практичным, но требует зарядки при длительном
простое — это делает deep sleep желательной, но не блокирующей функцией (ТЗ §10.2, ADR-009).

## 7. Проверки этапа аппаратного прототипа (ТЗ §35)

| № | Проверка | Критерий приёмки |
| --: | --- | --- |
| 1 | XIAO прошивается и виден в PlatformIO | `pio run -t upload` успешен, Serial отвечает |
| 2 | I²C-скан находит дисплей | Адрес `0x3C` (или `0x3D`) обнаружен |
| 3 | Дисплей выводит тестовый текст | Читаемо, без артефактов, контраст управляется |
| 4 | Датчик Холла даёт импульсы | При проносе магнита уровень меняется; счётчик растёт на 1 за проход |
| 5 | Отсутствие дребезга | На 10 медленных проходах ровно 10 импульсов (после фильтра) |
| 6 | Работа на скорости | На стенде/раскрутке колеса до эквивалента 60 км/ч импульсы не теряются |
| 7 | АКБ подключён, полярность верна | Устройство работает от батареи, напряжение соответствует |
| 8 | Зарядка работает | Ток заряда измерен, напряжение растёт, статус читается |
| 9 | Расчёт напряжения по ADC | Расхождение с мультиметром ≤ 50 мВ после калибровки |
| 10 | Зазор датчик–магнит | Устойчивое срабатывание при 2 и при 8 мм, без контакта |

Результаты фиксируются в `hardware/wiring.md` с фотографиями в `hardware/images/`.
| 11 | Автояркость OLED | Пять уровней без мерцания; средний ток LDR ≤20 мкА |

## 8. Известные аппаратные риски

| Риск | Проявление | Меры |
| --- | --- | --- |
| Ложные срабатывания от вибрации | Скачки скорости, завышенная дистанция | Цифровой Hall switch с гистерезисом, debounce, лимит скорости, фильтр «залипания» |
| Пропуск импульсов на высокой скорости | Заниженная скорость/дистанция | Ring buffer в ISR, отсутствие блокировок в основном цикле, проверка на стенде |
| Магнит смещается | Пропадание сигнала | Держатель с двойной фиксацией, контроль после первых 50 км |
| Просадка напряжения от OLED | Скачки процента заряда | EMA + компенсация IR-drop + монотонность процента |
| Некорректная распиновка АКБ в core | Неверный заряд или отсутствие показаний | Обязательная сверка `variant.h` + эмпирическая калибровка (§2.2) |
| Влага в корпусе | Отказ, коррозия | IP54, заглушка USB, конформное покрытие платы датчика |
| Обрыв кабеля датчика | Нет импульсов | Разъём JST, петля запаса, диагностика `SENSOR_NO_SIGNAL` в приложении |
| Обрыв/засветка LDR | Неверный контраст OLED | Проверка ADC range, fallback на пользовательский максимум, козырёк |
